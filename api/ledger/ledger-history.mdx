---
title: 'Ledger History'
api: 'GET https://{domain}.fincode.software/api/v6/services/ledger/account-ledger-history/{account_id}/{page}/{size}'
description: 'Retrieves paginated transaction history for a specific account.'
---

Fetches a paginated list of all ledger entries (transactions) for an account, providing a complete audit trail of account movements.

<RequestExample>
  ```bash cURL GET
  [https://{domain}.fincode.software/api/v6/services/ledger/account-ledger-history/{account_id}/{page}/{size}](https://{domain}.fincode.software/api/v6/services/ledger/account-ledger-history/{account_id}/{page}/{size})
  ```
</RequestExample>

## Request Headers

<ParamField header='X-Auth-Token' type='string' required>
  The **JWT Access Token** obtained from the `/login` or `/refresh-token` endpoint.
</ParamField>

<ParamField header='x-fapi-auth-date' type='string' required>
  The date and time at which the request was initiated (ISO 8601 format).
</ParamField>

<ParamField header='x-fapi-customer-ip-address' type='string' required>
  The IP address of the customer making the request.
</ParamField>

<ParamField header='x-fapi-interaction-id' type='string' required>
  Unique identifier for the interaction/session.
</ParamField>

## Path Parameters

<ParamField path='account_id' type='string' required>
  The unique identifier (UUID) of the account to retrieve history for.
</ParamField>

<ParamField path='page' type='number' required>
  Page number for pagination (starts from 0 or 1, depending on implementation).
</ParamField>

<ParamField path='size' type='number' required>
  Number of items per page.
</ParamField>

## Response

Returns a paginated list of ledger entries.

<ResponseField name='status' type='string' default='SUCCESS'>
  Overall status of the API request.
</ResponseField>

<ResponseField name='data' type='array'>
  List of ledger entries (transactions).
  <Expandable title='data array item'>
    <ResponseField name='id' type='string'>
      Unique identifier of the ledger entry.
    </ResponseField>
    <ResponseField name='accountIdentificationNumber' type='string'>
      Account identification number ID.
    </ResponseField>
    <ResponseField name='account' type='string'>
      Account ID.
    </ResponseField>
    <ResponseField name='transactionAmount' type='number'>
      Amount of the transaction.
    </ResponseField>
    <ResponseField name='previousAccountBalance' type='number'>
      Account balance before this transaction.
    </ResponseField>
    <ResponseField name='newBalance' type='number'>
      Account balance after this transaction.
    </ResponseField>
    <ResponseField name='currencyCode' type='string'>
      Currency code of the transaction.
    </ResponseField>
    <ResponseField name='ledgerType' type='string'>
      Type of ledger entry (e.g., `DEBIT`, `CREDIT`).
    </ResponseField>
    <ResponseField name='status' type='string'>
      Status of the transaction.
    </ResponseField>
    <ResponseField name='paymentReference' type='string'>
      Payment reference or transaction reference.
    </ResponseField>
    <ResponseField name='executionDate' type='string'>
      Date and time when the transaction was executed.
    </ResponseField>
    <ResponseField name='idempotencykey' type='string'>
      Idempotency key used for this transaction.
    </ResponseField>
    <ResponseField name='externalCorrelationId' type='string'>
      External correlation ID for tracking.
    </ResponseField>
    <ResponseField name='internalCorrelationId' type='string'>
      Internal correlation ID for tracking.
    </ResponseField>
    <ResponseField name='postingGroupId' type='string'>
      Posting group ID (for grouped transactions).
    </ResponseField>
    <ResponseField name='depositorFullName' type='string'>
      Name of the depositor or transaction initiator.
    </ResponseField>
    <ResponseField name='hostBankCorrelationId' type='string'>
      Host bank correlation ID (if applicable).
    </ResponseField>
    <ResponseField name='actionPerformedByUser' type='string'>
      User who performed the transaction.
    </ResponseField>
    <ResponseField name='clientId' type='string'>
      Client identifier.
    </ResponseField>
    <ResponseField name='tenancyId' type='string'>
      Tenancy identifier.
    </ResponseField>
    <ResponseField name='fundSenderAccountId' type='string'>
      Account ID of the sender (for transfers).
    </ResponseField>
    <ResponseField name='paymentAuthorisationId' type='string'>
      Payment authorization ID (if applicable).
    </ResponseField>
  </Expandable>
</ResponseField>

## Code Examples

<CodeGroup>

```bash cURL
curl --location 'https://{domain}.fincode.software/api/v6/services/ledger/account-ledger-history/123e4567-e89b-12d3-a456-426614174000/0/10' \
--header 'X-Auth-Token: YOUR_JWT_ACCESS_TOKEN' \
--header 'x-fapi-auth-date: 2024-01-15T10:30:00Z' \
--header 'x-fapi-customer-ip-address: 192.168.1.1' \
--header 'x-fapi-interaction-id: interaction-12345'
```

```javascript Node.js
const axios = require('axios');

const BASE_URL = 'https://{domain}.fincode.software/api/v6/services/ledger';

async function getLedgerHistory(accessToken, accountId, page = 0, size = 10) {
  try {
    const response = await axios.get(
      `${BASE_URL}/account-ledger-history/${accountId}/${page}/${size}`,
      {
        headers: {
          'X-Auth-Token': accessToken,
          'x-fapi-auth-date': new Date().toISOString(),
          'x-fapi-customer-ip-address': '192.168.1.1',
          'x-fapi-interaction-id': `interaction-${Date.now()}`,
        },
      }
    );

    console.log(`Found ${response.data.data.length} transactions:`);
    response.data.data.forEach((entry) => {
      console.log(
        `${entry.executionDate}: ${entry.ledgerType} ` +
        `${entry.transactionAmount} (Balance: ${entry.newBalance})`
      );
    });

    return response.data.data;
  } catch (error) {
    console.error(
      'Failed to fetch ledger history:',
      error.response?.data || error.message
    );
    throw error;
  }
}

// Usage Example
const accessToken = 'YOUR_JWT_ACCESS_TOKEN';
const accountId = '123e4567-e89b-12d3-a456-426614174000';
getLedgerHistory(accessToken, accountId, 0, 10);
```

</CodeGroup>

