---
title: "Process Card Payment"
api: "POST https://remitjunction.fincode.software/api/v6/services/paymentmanagement/process-card-payment"
description: "Process a card payment by providing card details, transaction amount, and request type. Returns the payment gateway response."
---

## Overview

This endpoint allows a customer to **process a payment via card** by providing necessary card details.  

Supported payment flows include:

- Authorizing a **card payment**  
- Returning detailed **transaction response** including status, issuer info, and merchant details  

This API is primarily used by dashboards, mobile apps, and payment portals to process payments directly via card.

<Note>
  Ensure card details are valid and payment gateway is properly configured.
</Note>

---

## Authentication

<ParamField header="X-Auth-Token" type="string" required>
  Authentication token obtained after login.
</ParamField>

<ParamField header="uuid" type="string" required>
  Device UUID used for request tracking.
</ParamField>

<ParamField header="platform" type="string" required>
  Platform identifier (e.g., "WEB", "ANDROID", "IOS").
</ParamField>

---

## Request Body

<ParamField body="currencyiso3a" type="string" required>
  3-letter ISO currency code of the transaction (e.g., "USD", "NGN").
</ParamField>

<ParamField body="pan" type="string" required>
  Primary Account Number (card number) of the customer.
</ParamField>

<ParamField body="expirydate" type="string" required>
  Card expiry date in `MM/yyyy` format.
</ParamField>

<ParamField body="securitycode" type="string" required>
  Card CVV/security code.
</ParamField>

<ParamField body="requesttypedescriptions" type="string" required>
  Description of the request type (e.g., "purchase", "refund").
</ParamField>

<ParamField body="baseamount" type="string" required>
  Amount to charge the card.
</ParamField>

<ParamField body="pcn" type="string">
  Optional payment code number associated with the transaction.
</ParamField>

---

## Response Fields

<ResponseField name="status" type="enum" required>
  API request status: `SUCCESS` or `FAILED`
</ResponseField>

<ResponseField name="message" type="string">
  Error description if request failed.
</ResponseField>

<ResponseField name="responseCode" type="number">
  HTTP-style response code: `200`, `401`, `500`, etc.
</ResponseField>

<ResponseField name="errorCode" type="string">
  Payment processing result or error code.
</ResponseField>

<ResponseField name="data" type="object" required>

  <Expandable title="CardPaymentResponse">

  <ResponseField name="requestreference" type="string">
    Reference ID for the payment request.
  </ResponseField>

  <ResponseField name="version" type="string">
    API or protocol version used.
  </ResponseField>

  <ResponseField name="secrand" type="string">
    Security random string used in payment processing.
  </ResponseField>

  <ResponseField name="response" type="array">
    Array of response objects from the payment gateway.

  <Expandable title="Response Object">

  <ResponseField name="transactionstartedtimestamp" type="string">
    Timestamp when the transaction started.
  </ResponseField>

  <ResponseField name="livestatus" type="string">
    Live status of the transaction (e.g., `SUCCESS`, `FAILED`).
  </ResponseField>

  <ResponseField name="issuer" type="string">
    Issuing bank of the card.
  </ResponseField>

  <ResponseField name="splitfinalnumber" type="string">
    Last digits of card number after masking.
  </ResponseField>

  <ResponseField name="dccenabled" type="string">
    Indicates if Dynamic Currency Conversion is enabled.
  </ResponseField>

  <ResponseField name="settleduedate" type="string">
    Payment settlement due date.
  </ResponseField>

  <ResponseField name="errorcode" type="string">
    Error code returned by the payment gateway if any.
  </ResponseField>

  <ResponseField name="baseamount" type="string">
    Amount charged in base currency.
  </ResponseField>

  <ResponseField name="tid" type="string">
    Terminal ID used for transaction.
  </ResponseField>

  <ResponseField name="merchantnumber" type="string">
    Merchant account number.
  </ResponseField>

  <ResponseField name="merchantcountryiso2a" type="string">
    Merchant country ISO code.
  </ResponseField>

  <ResponseField name="transactionreference" type="string">
    Transaction reference generated by payment gateway.
  </ResponseField>

  <ResponseField name="merchantname" type="string">
    Merchant name.
  </ResponseField>

  <ResponseField name="paymenttypedescription" type="string">
    Description of the payment type.
  </ResponseField>

  <ResponseField name="orderreference" type="string">
    Merchant order reference.
  </ResponseField>

  <ResponseField name="accounttypedescription" type="string">
    Type of card account (e.g., credit, debit).
  </ResponseField>

  <ResponseField name="acquirerresponsecode" type="string">
    Response code from acquiring bank.
  </ResponseField>

  <ResponseField name="requesttypedescription" type="string">
    Description of the request type.
  </ResponseField>

  <ResponseField name="securityresponsesecuritycode" type="string">
    Security response code.
  </ResponseField>

  <ResponseField name="currencyiso3a" type="string">
    Currency used for transaction.
  </ResponseField>

  <ResponseField name="authcode" type="string">
    Authorization code from the card issuer.
  </ResponseField>

  <ResponseField name="errormessage" type="string">
    Error message if any.
  </ResponseField>

  <ResponseField name="operatorname" type="string">
    Operator handling the transaction.
  </ResponseField>

  <ResponseField name="securityresponsepostcode" type="string">
    Postcode verification response.
  </ResponseField>

  <ResponseField name="maskedpan" type="string">
    Masked card number for security.
  </ResponseField>

  <ResponseField name="securityresponseaddress" type="string">
    Address verification response.
  </ResponseField>

  <ResponseField name="issuercountryiso2a" type="string">
    Issuer country ISO code.
  </ResponseField>

  <ResponseField name="settlestatus" type="string">
    Settlement status of the transaction.
  </ResponseField>

  </Expandable>
  </ResponseField>

  </Expandable>
</ResponseField>

---

## Request Example

<CodeGroup>

```bash cURL
curl --request POST \
  --url "https://remitjunction.fincode.software/api/v6/services/paymentmanagement/process-card-payment" \
  --header 'Content-Type: application/json' \
  --header 'X-Auth-Token: your_auth_token' \
  --header 'uuid: your_device_uuid' \
  --header 'platform: WEB' \
  --data '{
    "currencyiso3a": "USD",
    "pan": "4111111111111111",
    "expirydate": "12/2025",
    "securitycode": "123",
    "requesttypedescriptions": "purchase",
    "baseamount": "150.00",
    "pcn": "PCN12345"
  }'
````

```javascript JavaScript
const response = await fetch(
  "https://remitjunction.fincode.software/api/v6/services/paymentmanagement/process-card-payment",
  {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      "X-Auth-Token": authToken,
      "uuid": deviceUuid,
      "platform": "WEB"
    },
    body: JSON.stringify({
      currencyiso3a: "USD",
      pan: "4111111111111111",
      expirydate: "12/2025",
      securitycode: "123",
      requesttypedescriptions: "purchase",
      baseamount: "150.00",
      pcn: "PCN12345"
    })
  }
);

const data = await response.json();
```

```python Python
import requests

url = "https://remitjunction.fincode.software/api/v6/services/paymentmanagement/process-card-payment"
headers = {
    "Content-Type": "application/json",
    "X-Auth-Token": "your_auth_token",
    "uuid": "your_device_uuid",
    "platform": "WEB"
}
payload = {
    "currencyiso3a": "USD",
    "pan": "4111111111111111",
    "expirydate": "12/2025",
    "securitycode": "123",
    "requesttypedescriptions": "purchase",
    "baseamount": "150.00",
    "pcn": "PCN12345"
}

response = requests.post(url, json=payload, headers=headers)
data = response.json()
```

</CodeGroup>

---

## Response Example

<CodeGroup>

```json 200 OK
{
  "status": "SUCCESS",
  "responseCode": 200,
  "errorCode": "Payment successfully",
  "data": {
    "requestreference": "REQ12345",
    "version": "1.0",
    "secrand": "ABC123XYZ",
    "response": [
      {
        "transactionstartedtimestamp": "2025-12-05T10:15:30",
        "livestatus": "SUCCESS",
        "issuer": "Bank of Example",
        "splitfinalnumber": "1111",
        "dccenabled": "No",
        "settleduedate": "2025-12-07",
        "errorcode": "00",
        "baseamount": "150.00",
        "tid": "TID12345",
        "merchantnumber": "M123456",
        "merchantcountryiso2a": "US",
        "transactionreference": "TRX12345",
        "merchantname": "Example Store",
        "paymenttypedescription": "Card Payment",
        "orderreference": "ORD12345",
        "accounttypedescription": "Credit",
        "acquirerresponsecode": "00",
        "requesttypedescription": "purchase",
        "securityresponsesecuritycode": "123",
        "currencyiso3a": "USD",
        "authcode": "AUTH9876",
        "errormessage": "",
        "operatorname": "Operator1",
        "securityresponsepostcode": "12345",
        "maskedpan": "411111******1111",
        "securityresponseaddress": "Verified",
        "issuercountryiso2a": "US",
        "settlestatus": "SETTLED"
      }
    ]
  }
}
```

```json 401 Unauthorized
{
  "status": "FAILED",
  "message": "Invalid device info.",
  "responseCode": 401,
  "errorCode": "401"
}
```

```json 500 Error
{
  "status": "FAILED",
  "message": "Card payment failed.",
  "responseCode": 500,
  "errorCode": "500"
}
```

</CodeGroup>

---

## Use Cases

### 1. Process a Card Payment

```javascript
await processCardPayment({
  currencyiso3a: "USD",
  pan: "4111111111111111",
  expirydate: "12/2025",
  securitycode: "123",
  requesttypedescriptions: "purchase",
  baseamount: "150.00"
});
```

---

### 2. Process Payment With Optional PCN

```javascript
await processCardPayment({
  currencyiso3a: "USD",
  pan: "4111111111111111",
  expirydate: "12/2025",
  securitycode: "123",
  requesttypedescriptions: "purchase",
  baseamount: "150.00",
  pcn: "PCN12345"
});
```

---

## Error Handling

<ResponseExample>

```javascript
try {
  const res = await fetch("/process-card-payment", { ...config });
  const data = await res.json();

  if (data.status === "FAILED") {
    console.error("Error:", data.message);
  }
} catch (err) {
  console.error("Network/server error:", err);
}
```

</ResponseExample>

---

## Important Notes

<Warning>
  - Authentication is mandatory  
  - Required fields: `currencyiso3a`, `pan`, `expirydate`, `securitycode`, `requesttypedescriptions`, `baseamount`  
</Warning>

<Info>
  - Optional field `pcn` can be used for associating a payment code  
  - Ensure card and payment gateway details are correct  
</Info>

---

## Next Steps

<CardGroup cols={2}>
  <Card title="Process Payment With Token" href="/api/deposit-engine/processpaymentwithpaymenttoken">
    Fund wallet using payment token.
  </Card>

  <Card title="Payment Method Charges" href="/api/deposit-engine/payment-methods-charges">
    Retrieve applicable charges for payments.
  </Card>

  <Card title="Bank I Will Pay Later" href="/api/deposit-engine/bank-iwillpaylater">
    Mark a payment as 'I will pay later'.
  </Card>

  <Card title="Notify Bank Transfer Payment" href="/api/deposit-engine/notify-bank-transfer-payment">
    Alert system about customer bank transfer.
  </Card>
</CardGroup>
````