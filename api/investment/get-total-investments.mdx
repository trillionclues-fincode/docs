---
title: 'Get Total Investments'
api: 'GET https://test-investment-products-service.fincode.software/api/v1/user/get-total/{user-id}'
description: 'Retrieves dashboard information including total investments and interest accrued for a user.'
---

Fetches comprehensive dashboard information for a user, including their total investments across all products and the total interest accrued.

<RequestExample>
  ```bash cURL
  curl --request GET 'https://test-investment-products-service.fincode.software/api/v1/user/get-total/8ecca811-607f-44e1-a57e-cd856c50437d' \
  --header 'Authorization: Bearer <token>'
  ```
</RequestExample>

## Request Headers

<ParamField header='X-Auth-Token' type='string' required>
  The **JWT Access Token** obtained from the `/login` or `/refresh-token` endpoint.
</ParamField>

<ParamField header='x-idempotency-key' type='string' required>
  Unique idempotency key for the request to prevent duplicate processing.
</ParamField>

<ParamField header='x-fapi-auth-date' type='string' required>
  The date and time at which the request was initiated (ISO 8601 format).
</ParamField>

<ParamField header='x-fapi-customer-ip-address' type='string' required>
  The IP address of the customer making the request.
</ParamField>

<ParamField header='x-fapi-interaction-id' type='string' required>
  Unique identifier for the interaction/session.
</ParamField>

## Path Parameters

<ParamField path='user-id' type='string' required>
  The unique identifier (UUID) of the user whose investment dashboard information to retrieve.
</ParamField>

## Response

Returns dashboard information with total investments and interest accrued.

<ResponseField name='status' type='string' default='SUCCESS'>
  Overall status of the API request.
</ResponseField>

<ResponseField name='data' type='object'>
  Dashboard information object.
  <Expandable title='data object'>
    <ResponseField name='totalInvestmentsResponseDto' type='object'>
      Total investments information.
      <Expandable title='totalInvestmentsResponseDto object'>
        <ResponseField name='totalInvestments' type='number'>
          Total amount invested across all products.
        </ResponseField>
        <ResponseField name='currencyCode' type='string'>
          Currency code for the investments (e.g., `NGN`, `USD`).
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name='interestAccruedResponseDto' type='object'>
      Interest accrued information.
      <Expandable title='interestAccruedResponseDto object'>
        <ResponseField name='totalInvestments' type='number'>
          Total interest accrued on all investments.
        </ResponseField>
        <ResponseField name='currencyCode' type='string'>
          Currency code for the interest (e.g., `NGN`, `USD`).
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

## Code Examples

<CodeGroup>

```bash cURL
curl --location 'https://test-investment-products-service.fincode.software/api/v1/user/get-total/123e4567-e89b-12d3-a456-426614174000' \
--header 'X-Auth-Token: YOUR_JWT_ACCESS_TOKEN' \
--header 'x-idempotency-key: unique-key-12345' \
--header 'x-fapi-auth-date: 2024-01-15T10:30:00Z' \
--header 'x-fapi-customer-ip-address: 192.168.1.1' \
--header 'x-fapi-interaction-id: interaction-12345'
```

```javascript Node.js
const axios = require('axios');

const BASE_URL = 'https://test-investment-products-service.fincode.software/api/v1/user';

async function getTotalInvestments(accessToken, userId) {
  try {
    const response = await axios.get(
      `${BASE_URL}/get-total/${userId}`,
      {
        headers: {
          'X-Auth-Token': accessToken,
          'x-idempotency-key': `key-${Date.now()}`,
          'x-fapi-auth-date': new Date().toISOString(),
          'x-fapi-customer-ip-address': '192.168.1.1',
          'x-fapi-interaction-id': `interaction-${Date.now()}`,
        },
      }
    );

    const dashboard = response.data.data;
    console.log('Total Investments:', dashboard.totalInvestmentsResponseDto.totalInvestments);
    console.log('Interest Accrued:', dashboard.interestAccruedResponseDto.totalInvestments);
    return dashboard;
  } catch (error) {
    console.error(
      'Failed to fetch total investments:',
      error.response?.data || error.message
    );
    throw error;
  }
}

// Usage Example
const accessToken = 'YOUR_JWT_ACCESS_TOKEN';
const userId = '123e4567-e89b-12d3-a456-426614174000';
getTotalInvestments(accessToken, userId);
```

</CodeGroup>

